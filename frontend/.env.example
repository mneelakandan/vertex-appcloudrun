# URL of the backend Cloud Run service
# For local dev, point to local backend
REACT_APP_BACKEND_URL=http://localhost:8080

# In production, this is injected at Docker build time via Cloud Build:
# REACT_APP_BACKEND_URL=https://vertex-app-backend-xxxx-uc.a.run.app
